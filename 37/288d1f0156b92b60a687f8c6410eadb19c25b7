Task: Update backend/main.py to use dynamic CORS origins based on the detected environment.\nSummary: Updated backend/main.py to use config.IS_CODESPACE and config.DEBUG_MODE. If active, allow_origins is set to ["*"], enabling flexible connections in GitHub Codespaces. Added backend/tests/test_cors.py with TestClient to verify behavior.\nFiles: backend/main.py, backend/tests/test_cors.py\nWhy: To resolve CORS preflight errors when the frontend is accessed via a dynamic Codespaces URL.
